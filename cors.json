{
  "rules": [
    {
      "allowed": {
        "origins": [
          "http://localhost:3000",
          "https://*.vercel.app",
          "https://*.identy.cloud"
        ],
        "methods": ["GET", "PUT", "POST", "DELETE"],
        "headers": ["*"]
      },
      "maxAgeSeconds": 3000
    }
  ]
}
